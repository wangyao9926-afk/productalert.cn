# Leave empty for local Vite proxy development.
# Set to the public HTTPS API origin in Netlify production, without /api.
VITE_API_BASE_URL=

# Demo fallback is only useful during frontend design. Keep false in Netlify production.
VITE_ENABLE_DEMO_FALLBACK=false
